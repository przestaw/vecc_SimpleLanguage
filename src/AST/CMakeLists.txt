file(GLOB AST_SRC "${PROJECT_SOURCE_DIR}/src/AST/general/*.cpp")
file(GLOB AST_STATEMENT_SRC "${PROJECT_SOURCE_DIR}/src/AST/statement/*.cpp")
file(GLOB AST_EXPRESSION_SRC "${PROJECT_SOURCE_DIR}/src/AST/expression/*.cpp")

set(LIB_SRC ${LIB_SRC} ${AST_SRC} ${AST_STATEMENT_SRC} ${AST_EXPRESSION_SRC} PARENT_SCOPE)